# 🏗 ООП в Java: конструкторы

## 📝 Краткая форма

- **Конструктор** — это специальный метод класса, который вызывается **при создании объекта**.  
- Конструктор **инициализирует поля объекта** и имеет **то же имя, что и класс**.  
- Можно создавать **несколько конструкторов** (перегрузка), чтобы создавать объекты с разными начальными данными.  
- Конструкторы **не имеют типа возвращаемого значения** (даже `void`).  

---

## 📚 Подробная теория

### 1) Создание конструктора
```java
public class Transport {

    private float speed, weight;
    private String color;
    private byte[] coordinate;

    // Конструктор класса
    public Transport(float speed, float weight, String color, byte[] coordinate) {
        this.speed = speed;
        this.weight = weight;
        this.color = color;
        this.coordinate = coordinate;
    }
}
При создании объекта с параметрами:

java
Копировать код
Transport bmw = new Transport(100.5f, 2500, "Red", new byte[]{0, 0, 0});
Поля объекта инициализируются автоматически при создании.

2) Использование метода setValues внутри конструктора
java
Копировать код
public Transport(float speed, float weight, String color, byte[] coordinate) {
    setValues(speed, weight, color, coordinate);
}

// Метод для установки значений
public void setValues(float speed, float weight, String color, byte[] coordinate) {
    this.speed = speed;
    this.weight = weight;
    this.color = color;
    this.coordinate = coordinate;
}
Это позволяет использовать один код для установки значений как в конструкторе, так и отдельно через метод.

3) Перегрузка конструкторов
Можно создавать несколько конструкторов с разными параметрами:

java
Копировать код
public Transport() {
    // пустой конструктор — значения по умолчанию
    this.speed = 0;
    this.weight = 0;
    this.color = "Unknown";
    this.coordinate = new byte[]{0,0,0};
}

public Transport(float speed, float weight) {
    this.speed = speed;
    this.weight = weight;
    this.color = "Default";
    this.coordinate = new byte[]{0,0,0};
}
Позволяет создавать объекты разными способами, удобными для программы.

4) Подводные моменты
Конструктор всегда имеет то же имя, что и класс.

Конструктор не возвращает значение.

Если не объявить конструктор, компилятор создаёт пустой конструктор по умолчанию.

Поля класса рекомендуется делать private и управлять ими через геттеры и сеттеры (инкапсуляция).

💻 Полный пример кода
java
Копировать код
public class Transport {

    private float speed, weight;
    private String color;
    private byte[] coordinate;

    // Конструктор с параметрами
    public Transport(float speed, float weight, String color, byte[] coordinate) {
        setValues(speed, weight, color, coordinate);
    }

    // Метод для установки значений
    public void setValues(float speed, float weight, String color, byte[] coordinate) {
        this.speed = speed;
        this.weight = weight;
        this.color = color;
        this.coordinate = coordinate;
    }

    // Геттеры для доступа к полям (опционально)
    public float getSpeed() { return speed; }
    public float getWeight() { return weight; }
    public String getColor() { return color; }
}
java
Копировать код
public class Main {
    public static void main(String[] args) {

        // Создание объекта с помощью конструктора
        Transport bmw = new Transport(100.5f, 2500, "Red", new byte[]{0, 0, 0});

        // Пример вывода данных объекта
        System.out.println("Цвет: " + bmw.getColor() + ", Скорость: " + bmw.getSpeed());
    }
}