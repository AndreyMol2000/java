# 🚗 Наследование классов в Java

## 📝 Краткая форма

- **Наследование** — это механизм ООП, который позволяет **создавать новый класс на основе существующего**.  
- Новый класс называется **подкласс (child class)**, а исходный — **родительский класс (superclass)**.  
- Подкласс **наследует поля и методы родителя**, но может добавлять свои или переопределять существующие.  
- Ключевое слово **`extends`** используется для наследования.  
- Ключевое слово **`super`** вызывает конструктор родителя или обращается к его методам.  

---

## 📚 Подробная теория

### 1) Родительский класс
```java
public class Transport {

    private float speed, weight;
    private String color;
    private byte[] coordinate;

    // Конструктор с параметрами
    public Transport(float speed, float weight, String color, byte[] coordinate) {
        setValues(speed, weight, color, coordinate);
    }

    // Пустой конструктор
    public Transport() {}

    // Метод для установки значений
    public void setValues(float speed, float weight, String color, byte[] coordinate) {
        this.speed = speed;
        this.weight = weight;
        this.color = color;
        this.coordinate = coordinate;
    }

    // Геттеры для доступа к полям (инкапсуляция)
    public float getSpeed() { return speed; }
    public float getWeight() { return weight; }
    public String getColor() { return color; }
}
2) Подкласс (наследник)
java
Копировать код
public class Car extends Transport {

    // Конструктор подкласса
    public Car(float speed, float weight, String color, byte[] coordinate) {
        super(speed, weight, color, coordinate); // вызов конструктора родителя
    }

    // Можно добавлять новые методы и поля
}
Ключевое слово super(...) вызывает конструктор родителя, чтобы правильно инициализировать наследуемые поля.

Подкласс автоматически получает все публичные и защищённые (protected) методы и поля родителя.

Можно добавлять свои поля и методы, например model, brand, drive().

3) Использование наследования
java
Копировать код
public class Main {
    public static void main(String[] args) {

        // Создание объекта подкласса
        Car bmw = new Car(100.5f, 2500, "Red", new byte[]{0, 0, 0});

        // Используем методы родителя
        System.out.println("Цвет: " + bmw.getColor());
        System.out.println("Скорость: " + bmw.getSpeed());
    }
}
Объект bmw имеет все свойства и методы Transport, даже если они не определены в Car.

4) Подводные моменты
Наследуются только public и protected поля/методы.

private поля родителя недоступны напрямую, но через методы (геттеры/сеттеры) можно работать с ними.

Конструкторы не наследуются, их нужно определять в подклассе и при необходимости вызывать super().

Наследование помогает повторно использовать код и расширять функциональность.

💻 Полный пример кода
java
Копировать код
// Родительский класс Transport
public class Transport {

    private float speed, weight;
    private String color;
    private byte[] coordinate;

    public Transport(float speed, float weight, String color, byte[] coordinate) {
        setValues(speed, weight, color, coordinate);
    }

    public Transport() {}

    public void setValues(float speed, float weight, String color, byte[] coordinate) {
        this.speed = speed;
        this.weight = weight;
        this.color = color;
        this.coordinate = coordinate;
    }

    public float getSpeed() { return speed; }
    public float getWeight() { return weight; }
    public String getColor() { return color; }
}

// Подкласс Car
public class Car extends Transport {

    public Car(float speed, float weight, String color, byte[] coordinate) {
        super(speed, weight, color, coordinate);
    }

    // Можно добавить новые методы и поля
}

// Main
public class Main {
    public static void main(String[] args) {

        Car bmw = new Car(100.5f, 2500, "Red", new byte[]{0, 0, 0});

        System.out.println("Цвет: " + bmw.getColor());
        System.out.println("Скорость: " + bmw.getSpeed());
    }
}