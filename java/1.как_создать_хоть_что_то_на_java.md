# 🚀 Как создать что-то на Java

## 📂 Структура Java-программы

```java
public class Main {
    // public доступ
    // class — мы создаем новый класс
    public static void main(String[] args){
        // static — доступ без создания объекта класса
        // void — функция ничего не возвращает

        System.out.print("беги");
        // System — базовый класс (системные функции)
        // out — объект вывода
        // метод print отображает текст в одну строку (без переноса)

        System.out.println("новая строчка");
        System.out.println("новая строчка");
        // println — выводит текст и добавляет перенос строки

        System.out.println("новая \\ \n \t строчка \"");
        // \n — перенос строки
        // \t — табуляция (отступ)
        // \" — вывод кавычки
        // \\ — вывод обратного слэша

        /*
        многострочный комментарий
         */
    }
}
