# 🏗 Абстрактные классы и методы в Java

## 📝 Краткая форма

- **Абстрактный класс** — это класс, который **не предназначен для создания объектов напрямую**, а служит **основой для других классов**.  
- **Абстрактный метод** — это метод без реализации, который **обязателен для переопределения** в подклассе.  
- Ключевое слово `abstract` используется для **классов и методов**.  
- Абстрактный класс может содержать **реализованные методы** и **поля**.  

---

## 📚 Подробная теория

### 1) Создание абстрактного класса
```java
public abstract class Transport {

    private float speed, weight;
    private String color;
    private byte[] coordinate;

    // Конструктор с параметрами
    public Transport(float speed, float weight, String color, byte[] coordinate) {
        setValues(speed, weight, color, coordinate);
    }

    public Transport() {}

    // Метод для установки значений
    public void setValues(float speed, float weight, String color, byte[] coordinate) {
        this.speed = speed;
        this.weight = weight;
        this.color = color;
        this.coordinate = coordinate;
    }

    // Абстрактный метод — должен быть реализован в подклассе
    public abstract void move(float speed);
}
Абстрактные методы не имеют тела.

Любой класс, который наследует абстрактный, должен реализовать все абстрактные методы.

Абстрактный класс может содержать реализованные методы и поля, чтобы общие функции использовать сразу.

2) Подкласс, реализующий абстрактный метод
java
Копировать код
public class Car extends Transport {

    public Car(float speed, float weight, String color, byte[] coordinate) {
        super(speed, weight, color, coordinate);
    }

    // Реализация обязательного абстрактного метода
    @Override
    public void move(float speed) {
        System.out.println("Автомобиль движется со скоростью " + speed);
    }

    // Переопределение метода setValues для кастомных действий
    @Override
    public void setValues(float speed, float weight, String color, byte[] coordinate) {
        super.setValues(speed, weight, color, coordinate);
        System.out.println("Данные автомобиля обновлены");
    }
}
Метод move обязателен для реализации в классе Car.

Можно дополнять методы родителя, используя super.

3) Использование абстрактного класса
java
Копировать код
public class Main {
    public static void main(String[] args) {

        // Нельзя создать объект абстрактного класса
        // Transport t = new Transport(); // ошибка!

        // Создаем объект подкласса
        Car bmw = new Car(100.5f, 2500, "Red", new byte[]{0, 0, 0});

        // Используем методы
        bmw.move(120); // вызов реализованного абстрактного метода
        bmw.setValues(120, 2400, "Blue", new byte[]{1, 2, 3});
    }
}
Объект создается только через подкласс, который реализует все абстрактные методы.

Это обеспечивает обязательную структуру для всех наследников.

4) Подводные моменты
Абстрактный класс может содержать как реализованные, так и абстрактные методы.

Абстрактный метод не может быть private — иначе наследник не сможет его переопределить.

Абстрактные классы помогают создавать общее ядро для похожих классов и задавать обязательные методы.

💻 Полный пример кода
java
Копировать код
// Абстрактный класс Transport
public abstract class Transport {

    private float speed, weight;
    private String color;
    private byte[] coordinate;

    public Transport(float speed, float weight, String color, byte[] coordinate) {
        setValues(speed, weight, color, coordinate);
    }

    public Transport() {}

    public void setValues(float speed, float weight, String color, byte[] coordinate) {
        this.speed = speed;
        this.weight = weight;
        this.color = color;
        this.coordinate = coordinate;
    }

    public abstract void move(float speed);
}

// Подкласс Car
public class Car extends Transport {

    public Car(float speed, float weight, String color, byte[] coordinate) {
        super(speed, weight, color, coordinate);
    }

    @Override
    public void move(float speed) {
        System.out.println("Автомобиль движется со скоростью " + speed);
    }

    @Override
    public void setValues(float speed, float weight, String color, byte[] coordinate) {
        super.setValues(speed, weight, color, coordinate);
        System.out.println("Данные автомобиля обновлены");
    }
}

// Main
public class Main {
    public static void main(String[] args) {
        Car bmw = new Car(100.5f, 2500, "Red", new byte[]{0, 0, 0});
        bmw.move(120);
        bmw.setValues(120, 2400, "Blue", new byte[]{1, 2, 3});
    }
}