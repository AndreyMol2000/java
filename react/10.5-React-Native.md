# Продвинутые темы: React Native

## 🔹 Кратко
- **React Native** — фреймворк для создания **мобильных приложений** на iOS и Android с использованием React.  
- Компоненты React рендерятся в нативные элементы платформы.  
- Позволяет использовать один код для обеих платформ.

---

## 🔹 Подробно

### 1. Установка и настройка
- Для начала используем **Expo** (проще для новичков):
```bash
npm install -g expo-cli
expo init my-app
cd my-app
npm start
Проект можно запустить на эмуляторе или реальном устройстве через приложение Expo Go.

2. Основные компоненты
Компонент	Назначение
<View>	Контейнер, аналог <div>
<Text>	Отображение текста
<TextInput>	Поля ввода
<Button>	Кнопка
<ScrollView>	Скроллируемая область
<FlatList>	Список элементов

3. Пример простого приложения
jsx
Копировать код
import React, { useState } from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

export default function App() {
  const [count, setCount] = useState(0);

  return (
    <View style={styles.container}>
      <Text>Счётчик: {count}</Text>
      <Button title="Увеличить" onPress={() => setCount(count + 1)} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  }
});
4. Навигация
Используется библиотека React Navigation:

bash
Копировать код
npm install @react-navigation/native
npm install @react-navigation/native-stack
Пример навигации:

jsx
Копировать код
import { NavigationContainer } from '@react-navigation/native';
import { createNativeStackNavigator } from '@react-navigation/native-stack';
import HomeScreen from './HomeScreen';
import DetailsScreen from './DetailsScreen';

const Stack = createNativeStackNavigator();

export default function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator>
        <Stack.Screen name="Home" component={HomeScreen}/>
        <Stack.Screen name="Details" component={DetailsScreen}/>
      </Stack.Navigator>
    </NavigationContainer>
  );
}
5. Практическое задание
Создать приложение с двумя экранами: Home и Profile.

На главном экране разместить кнопку для перехода на экран Profile.

Добавить счётчик на каждом экране.

Попробовать стилизовать компоненты с StyleSheet.

6. Советы и лучшие практики
Используйте Expo, если хотите быстро начать.

Для производственных приложений можно использовать React Native CLI.

Следите за производительностью, минимизируйте ререндер компонентов.

Для стилей и UI можно использовать NativeBase, React Native Paper, Tailwind RN.

🔹 Дополнительно
Это завершает продвинутый блок тем по React.

Дальше можно возвращаться к практике: хуки, роутинг, формы, API и оптимизация, используя эти продвинутые знания.