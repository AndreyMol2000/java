# Условный рендеринг в React

## 🔹 Кратко
Условный рендеринг позволяет **показывать или скрывать части интерфейса** в зависимости от состояния или props.  
В React используются **тернарные операторы, логические выражения и функции** для управления видимостью компонентов.

---

## 🔹 Подробно

### Способы условного рендеринга

1. **Тернарный оператор**
```jsx
{condition ? <ComponentA /> : <ComponentB />}
Если condition истинно, рендерится <ComponentA>, иначе <ComponentB>.

Логическое И (&&)

jsx
Копировать код
{condition && <Component />}
Компонент рендерится только если condition истинно.

Удобно для показа элементов при определённых условиях.

Функции для рендеринга

jsx
Копировать код
function renderContent(status) {
  if (status === "loading") return <p>Загрузка...</p>;
  if (status === "error") return <p>Ошибка!</p>;
  return <p>Данные загружены</p>;
}
🔹 Примеры кода
1. Тернарный оператор
jsx
Копировать код
function UserStatus({ isLoggedIn }) {
  return (
    <div>
      {isLoggedIn ? <p>Добро пожаловать!</p> : <p>Пожалуйста, войдите</p>}
    </div>
  );
}
2. Логическое И
jsx
Копировать код
function Notifications({ messages }) {
  return (
    <div>
      {messages.length > 0 && <p>У вас {messages.length} новых сообщений</p>}
    </div>
  );
}
3. Функция рендера
jsx
Копировать код
function StatusMessage({ status }) {
  const renderMessage = () => {
    if (status === "loading") return <p>Загрузка...</p>;
    if (status === "success") return <p>Данные загружены</p>;
    if (status === "error") return <p>Произошла ошибка</p>;
  };

  return <div>{renderMessage()}</div>;
}
4. Сложный пример с массивом и условием
jsx
Копировать код
function TodoList({ todos }) {
  return (
    <ul>
      {todos.length === 0
        ? <li>Список пуст</li>
        : todos.map(todo => <li key={todo.id}>{todo.text}</li>)}
    </ul>
  );
}
🔹 Практическое задание
Создать компонент UserStatus с пропом isLoggedIn и условным отображением сообщения.

Создать компонент Notifications, который показывает количество сообщений только если их > 0.

Сделать компонент StatusMessage с тремя состояниями: loading, success, error.

Создать компонент TodoList с пустым и заполненным массивом задач, использовать условный рендеринг для отображения "Список пуст".

🔹 Советы и лучшие практики
Используйте тернарный оператор для простых условий.

Используйте && для показа компонента только при истинном условии.

Для сложных условий лучше создавать отдельные функции рендера, чтобы код оставался читаемым.

Не смешивайте несколько методов в одном выражении — это снижает читаемость.

🔹 Дополнительно
Условный рендеринг часто используется с state, чтобы показывать загрузку, ошибки или контент.

После освоения условного рендеринга переходите к хукам React, чтобы управлять состоянием и эффектами компонентов динамически.