# Стилизация React-приложений: Styled-components

## 🔹 Кратко
- **Styled-components** — это библиотека для **CSS-in-JS**, позволяющая писать стили прямо в JavaScript.  
- Каждому компоненту автоматически присваивается уникальный класс.  
- Поддерживает **props**, **темы** и **анимации**.

---

## 🔹 Подробно

### 1. Установка
```bash
npm install styled-components
Для TypeScript дополнительно:

bash
Копировать код
npm install @types/styled-components
2. Создание Styled-компонента
jsx
Копировать код
import styled from "styled-components";

const Button = styled.button`
  background-color: #4caf50;
  color: white;
  padding: 10px 20px;
  border: none;
  border-radius: 5px;
  cursor: pointer;

  &:hover {
    background-color: #45a049;
  }
`;

function App() {
  return <Button>Нажми меня</Button>;
}

export default App;
3. Использование props для динамических стилей
jsx
Копировать код
const Button = styled.button`
  background-color: ${(props) => (props.primary ? "#4caf50" : "#f44336")};
  color: white;
  padding: 10px 20px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
`;

function App() {
  return (
    <div>
      <Button primary>Primary</Button>
      <Button>Secondary</Button>
    </div>
  );
}
4. Темизация
Styled-components поддерживает ThemeProvider:

jsx
Копировать код
import { ThemeProvider } from "styled-components";

const theme = {
  colors: {
    primary: "#4caf50",
    secondary: "#f44336"
  }
};

const Button = styled.button`
  background-color: ${(props) => props.theme.colors.primary};
  color: white;
`;

function App() {
  return (
    <ThemeProvider theme={theme}>
      <Button>Кнопка с темой</Button>
    </ThemeProvider>
  );
}
5. Практическое задание
Создать компонент Card с заголовком и кнопкой, используя Styled-components.

Сделать динамическое изменение цвета кнопки через props.

Создать тему с двумя цветами и использовать ThemeProvider.

Добавить hover-эффекты и анимацию при наведении.

6. Советы и лучшие практики
Используйте Styled-components для проектов, где важна изолированная стилизация.

Комбинируйте с CSS-модулями или Tailwind CSS для гибкости.

Для крупных приложений делите компоненты и стили на отдельные файлы.

🔹 Дополнительно
Следующая тема — 08.3-Tailwind-CSS.md, где рассмотрим утилитарный подход к стилям.