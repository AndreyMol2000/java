# Оптимизация рендеринга в React

## 🔹 Кратко
- Цель оптимизации — **сократить лишние рендеры компонентов** и повысить производительность.  
- Основные инструменты: `useMemo`, `useCallback`, структурирование компонентов и мемоизация.

---

## 🔹 Подробно

### 1. useMemo
- Меморизирует **вычисленное значение**, чтобы не пересчитывать его при каждом рендере.

```jsx
import React, { useState, useMemo } from "react";

function ExpensiveComponent({ number }) {
  const compute = (num) => {
    console.log("Вычисление...");
    return num * 2;
  };

  const result = useMemo(() => compute(number), [number]);

  return <p>Результат: {result}</p>;
}

function App() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <button onClick={() => setCount(count + 1)}>+1</button>
      <p>Счёт: {count}</p>
      <ExpensiveComponent number={10} />
    </div>
  );
}

export default App;
compute будет выполняться только при изменении number.

2. useCallback
Меморизирует функцию, чтобы не создавать новую при каждом рендере.

jsx
Копировать код
import React, { useState, useCallback } from "react";

const Child = React.memo(({ onClick }) => {
  console.log("Child рендерится");
  return <button onClick={onClick}>Нажми</button>;
});

function App() {
  const [count, setCount] = useState(0);

  const handleClick = useCallback(() => {
    alert("Клик!");
  }, []);

  return (
    <div>
      <p>Счёт: {count}</p>
      <button onClick={() => setCount(count + 1)}>+1</button>
      <Child onClick={handleClick} />
    </div>
  );
}

export default App;
Child не перерендерится, так как onClick мемоизирован через useCallback.

3. Структурирование компонентов
Разделяйте тяжёлые компоненты на более мелкие.

Используйте мемоизацию для компонентов, которые часто получают одинаковые props.

Избегайте создания функций и объектов в JSX без мемоизации (можно использовать useMemo и useCallback).

4. Практическое задание
Создать список пользователей с большим количеством элементов.

Применить React.memo для каждого элемента списка.

Использовать useMemo для вычисляемых значений (например, фильтры, сортировка).

Использовать useCallback для функций обработки событий.

Измерить производительность до и после оптимизации (React DevTools → Profiler).

5. Советы и лучшие практики
Оптимизируйте только узкие места, не стоит мемоизировать всё подряд.

Профилируйте приложение, чтобы понять, где реально нужны оптимизации.

Совмещайте React.memo, useMemo и useCallback для максимальной эффективности.

Следующая тема — 07.3-Ленивая-загрузка-компонентов-React.lazy-Suspense.md.