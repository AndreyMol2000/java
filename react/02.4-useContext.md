 useContext — глобальное состояние в React

## 🔹 Кратко
`useContext` — это **хук React**, который позволяет функциональным компонентам получать данные из **контекста** без необходимости передавать их через props на каждом уровне.

```jsx
const value = useContext(MyContext);
MyContext создается через React.createContext().

Компоненты могут подписываться на контекст и автоматически получать обновления.

🔹 Подробно
Основные моменты
Контекст позволяет делиться данными между компонентами, минуя цепочку props.

Используется для:

темы (theme)

авторизации (auth)

глобальных настроек

состояния приложения (state management)

Состоит из:

Provider — оборачивает компоненты и предоставляет значение.

Consumer или useContext — позволяет использовать значение внутри компонента.

🔹 Примеры кода
1. Создание контекста и Provider
jsx
Копировать код
import React, { createContext, useState } from "react";

export const ThemeContext = createContext();

export function ThemeProvider({ children }) {
  const [theme, setTheme] = useState("light");

  const toggleTheme = () => setTheme(prev => prev === "light" ? "dark" : "light");

  return (
    <ThemeContext.Provider value={{ theme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}
2. Использование useContext
jsx
Копировать код
import { useContext } from "react";
import { ThemeContext } from "./ThemeProvider";

function ThemedButton() {
  const { theme, toggleTheme } = useContext(ThemeContext);

  return (
    <button
      style={{
        background: theme === "light" ? "#fff" : "#333",
        color: theme === "light" ? "#000" : "#fff"
      }}
      onClick={toggleTheme}
    >
      Тема: {theme}
    </button>
  );
}
3. Подключение Provider
jsx
Копировать код
import React from "react";
import ReactDOM from "react-dom";
import { ThemeProvider } from "./ThemeProvider";
import App from "./App";

ReactDOM.render(
  <ThemeProvider>
    <App />
  </ThemeProvider>,
  document.getElementById("root")
);
🔹 Практическое задание
Создать контекст ThemeContext с темой light/dark.

Создать кнопку ThemedButton, которая меняет тему при клике.

Использовать useContext для получения темы и функции toggleTheme.

Проверить, что все дочерние компоненты реагируют на изменение темы.

🔹 Советы и лучшие практики
Контекст удобно использовать для глобальных данных, которые часто нужны нескольким компонентам.

Не используйте контекст для локального состояния, лучше использовать useState.

Оборачивайте Provider как можно выше в иерархии компонентов, чтобы охватить все нужные компоненты.

Можно создавать несколько контекстов для разных областей (theme, auth, settings).

🔹 Дополнительно
useContext не вызывает ререндер компонентов, которые не используют это значение.

Часто используют вместе с useReducer для управления сложным глобальным состоянием.

После освоения useContext переходите к useReducer и кастомным хукам для построения мощного стейт-менеджмента.