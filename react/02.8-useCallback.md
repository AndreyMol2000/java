# useCallback — мемоизация функций в React

## 🔹 Кратко
`useCallback` — это **хук React**, который позволяет **кэшировать функцию** между рендерами.  
Это полезно, когда функция передается в дочерние компоненты и их **не нужно ререндерить без необходимости**.

```jsx
const memoizedCallback = useCallback(() => {
  doSomething(a, b);
}, [a, b]);
doSomething — функция, которую нужно мемоизировать.

[a, b] — зависимости; функция будет пересоздаваться только при их изменении.

🔹 Подробно
Когда использовать
Для передачи функций в React.memo компоненты.

Для оптимизации обработчиков событий.

Для предотвращения лишних перерендеров дочерних компонентов.

Отличие от useMemo
useMemo мемоизирует значение, возвращаемое функцией.

useCallback мемоизирует саму функцию.

🔹 Примеры кода
1. Оптимизация дочернего компонента
jsx
Копировать код
import React, { useState, useCallback, memo } from "react";

const Button = memo(({ onClick, children }) => {
  console.log("Рендер Button:", children);
  return <button onClick={onClick}>{children}</button>;
});

function App() {
  const [count, setCount] = useState(0);

  const increment = useCallback(() => setCount(c => c + 1), []);

  return (
    <div>
      <p>Счётчик: {count}</p>
      <Button onClick={increment}>+1</Button>
    </div>
  );
}
2. Обработчики событий
jsx
Копировать код
function InputLogger() {
  const [text, setText] = useState("");

  const logText = useCallback(() => {
    console.log("Текст:", text);
  }, [text]);

  return (
    <div>
      <input value={text} onChange={e => setText(e.target.value)} />
      <button onClick={logText}>Лог текста</button>
    </div>
  );
}
🔹 Практическое задание
Создать компонент с кнопкой, передаваемой в дочерний компонент через props, мемоизировать функцию через useCallback.

Проверить рендеры дочернего компонента через console.log.

Попробовать изменить state, который не влияет на мемоизированную функцию — дочерний компонент не должен ререндериться.

Сравнить поведение с и без useCallback.

🔹 Советы и лучшие практики
Используйте useCallback только для функций, передаваемых в оптимизированные компоненты (React.memo).

Не мемоизируйте функции без необходимости, это не даст преимуществ.

Всегда указывайте правильные зависимости, иначе функция будет использовать устаревшие значения.

Часто комбинируется с useMemo для комплексной оптимизации компонентов.

🔹 Дополнительно
useCallback помогает предотвратить лишние перерендеры и оптимизировать производительность.

Вместе с useMemo и React.memo позволяет создавать эффективные компоненты с тяжелой логикой и списками.

После освоения useCallback переходите к работе с формами, роутингом и API, чтобы применять оптимизацию на практике.

yaml
Копировать код
