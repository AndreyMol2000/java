# Управление состоянием в React: Context API + useReducer

## 🔹 Кратко
- **Context API** позволяет передавать данные через дерево компонентов без проброса через props.  
- **useReducer** — хук для управления сложным состоянием, похож на Redux, но встроенный в React.  
- Вместе они позволяют создавать глобальное состояние приложения без внешних библиотек.

---

## 🔹 Подробно

### 1. Context API
- Создаем **контекст**, чтобы хранить состояние или функции.
```jsx
import { createContext, useContext, useState } from "react";

const ThemeContext = createContext();

export function ThemeProvider({ children }) {
  const [theme, setTheme] = useState("light");

  return (
    <ThemeContext.Provider value={{ theme, setTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}

// Использование в компоненте
function Header() {
  const { theme, setTheme } = useContext(ThemeContext);

  return (
    <header>
      <p>Текущая тема: {theme}</p>
      <button onClick={() => setTheme(theme === "light" ? "dark" : "light")}>
        Сменить тему
      </button>
    </header>
  );
}
2. useReducer
useReducer полезен, когда состояние сложное или зависит от нескольких действий.

jsx
Копировать код
import { useReducer } from "react";

const initialState = { count: 0 };

function reducer(state, action) {
  switch (action.type) {
    case "increment":
      return { count: state.count + 1 };
    case "decrement":
      return { count: state.count - 1 };
    default:
      return state;
  }
}

function Counter() {
  const [state, dispatch] = useReducer(reducer, initialState);

  return (
    <div>
      <p>Счёт: {state.count}</p>
      <button onClick={() => dispatch({ type: "increment" })}>+1</button>
      <button onClick={() => dispatch({ type: "decrement" })}>-1</button>
    </div>
  );
}
3. Context + useReducer
Можно объединять для создания глобального состояния.

jsx
Копировать код
import { createContext, useReducer, useContext } from "react";

const CounterContext = createContext();

const initialState = { count: 0 };

function reducer(state, action) {
  switch (action.type) {
    case "increment": return { count: state.count + 1 };
    case "decrement": return { count: state.count - 1 };
    default: return state;
  }
}

export function CounterProvider({ children }) {
  const [state, dispatch] = useReducer(reducer, initialState);
  return (
    <CounterContext.Provider value={{ state, dispatch }}>
      {children}
    </CounterContext.Provider>
  );
}

function CounterDisplay() {
  const { state, dispatch } = useContext(CounterContext);
  return (
    <div>
      <p>Счёт: {state.count}</p>
      <button onClick={() => dispatch({ type: "increment" })}>+1</button>
      <button onClick={() => dispatch({ type: "decrement" })}>-1</button>
    </div>
  );
}
4. Практическое задание
Создать глобальный контекст для темы (light/dark).

Создать useReducer для управления списком задач (добавление, удаление, отметка выполнено).

Обернуть приложение в Provider и использовать глобальные состояния в компонентах.

Попробовать комбинировать Context + useReducer для управления всеми состояниями приложения.

5. Советы и лучшие практики
Для небольших приложений Context + useState хватит.

Для сложных состояний (списки, формы, фильтры) лучше использовать useReducer.

Объединение Context + useReducer заменяет базовый Redux без установки сторонних библиотек.

После освоения Context + useReducer можно переходить к Redux и Redux Toolkit.

🔹 Дополнительно
useReducer может принимать функцию инициализации, чтобы задать начальное состояние динамически.

В больших приложениях создавайте отдельные файлы для контекстов и редьюсеров.

Следующая тема — 06.2-Redux-базовый-уровень.md.

yaml
Копировать код
