# Альтернативные стейт-менеджеры в React: Zustand, Jotai, Recoil

## 🔹 Кратко
- Помимо Redux, существуют лёгкие альтернативы для управления состоянием.  
- **Zustand**, **Jotai** и **Recoil** упрощают работу с глобальным состоянием, не требуя boilerplate.

---

## 🔹 Подробно

### 1. Zustand
- Лёгкий глобальный стор без провайдеров.  
- Создаётся с помощью функции `create`.

#### Установка
```bash
npm install zustand
Пример
jsx
Копировать код
import create from "zustand";

const useStore = create((set) => ({
  count: 0,
  increment: () => set((state) => ({ count: state.count + 1 })),
  decrement: () => set((state) => ({ count: state.count - 1 }))
}));

function Counter() {
  const { count, increment, decrement } = useStore();
  return (
    <div>
      <p>Счёт: {count}</p>
      <button onClick={increment}>+1</button>
      <button onClick={decrement}>-1</button>
    </div>
  );
}

export default Counter;
2. Jotai
"Atomic" state: каждая часть состояния — атом.

Легко использовать и масштабировать.

Установка
bash
Копировать код
npm install jotai
Пример
jsx
Копировать код
import { atom, useAtom } from "jotai";

const countAtom = atom(0);

function Counter() {
  const [count, setCount] = useAtom(countAtom);
  return (
    <div>
      <p>Счёт: {count}</p>
      <button onClick={() => setCount(count + 1)}>+1</button>
      <button onClick={() => setCount(count - 1)}>-1</button>
    </div>
  );
}

export default Counter;
3. Recoil
Управляет состоянием через атомы и селекторы.

Подходит для сложных приложений с зависимым состоянием.

Установка
bash
Копировать код
npm install recoil
Пример
jsx
Копировать код
import { atom, useRecoilState, RecoilRoot } from "recoil";

const countState = atom({ key: "countState", default: 0 });

function Counter() {
  const [count, setCount] = useRecoilState(countState);
  return (
    <div>
      <p>Счёт: {count}</p>
      <button onClick={() => setCount(count + 1)}>+1</button>
      <button onClick={() => setCount(count - 1)}>-1</button>
    </div>
  );
}

function App() {
  return (
    <RecoilRoot>
      <Counter />
    </RecoilRoot>
  );
}

export default App;
4. Практическое задание
Создать счётчик в Zustand и сравнить с Redux Toolkit.

Попробовать создать глобальный список задач в Jotai.

Реализовать компонент с Recoil, который обновляет зависимые состояния.

Попробовать комбинации атомов и селекторов для вычисляемых значений.

5. Советы и лучшие практики
Выбирайте инструмент под задачу:

Zustand — лёгкий стор без boilerplate

Jotai — атомарные состояния, легко масштабировать

Recoil — сложные зависимости между состояниями

Для небольших проектов часто достаточно Zustand.

Для сложных приложений с большим количеством данных — Redux Toolkit или Recoil.

Всегда отделяйте глобальное и локальное состояние.

🔹 Дополнительно
Zustand и Jotai не требуют провайдера (Recoil требует RecoilRoot).

Можно сочетать с React Query для кэширования и асинхронных запросов.

Следующая большая тема — 07. Оптимизация.