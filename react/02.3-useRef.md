# useRef — работа с DOM и сохранение значений

## 🔹 Кратко
`useRef` — это **хук React**, который позволяет:
1. Получать **доступ к DOM-элементам**.
2. Сохранять **значения между рендерами**, не вызывая повторного рендера.

```jsx
const refContainer = useRef(initialValue);
refContainer.current — текущее значение.

Изменение refContainer.current не вызывает ререндер.

🔹 Подробно
Основные случаи использования
Доступ к DOM-элементу

Например, для фокуса на input или измерения элемента.

Хранение переменной между рендерами

Удобно для таймеров, счетчиков, предыдущего состояния.

Отличие от state
Характеристика	useState	useRef
Сохранение данных	✅ Да	✅ Да
Вызывает ререндер	✅ Да	❌ Нет
Использование	UI и логика	DOM, значения между рендерами

🔹 Примеры кода
1. Доступ к input и установка фокуса
jsx
Копировать код
import { useRef } from "react";

function FocusInput() {
  const inputRef = useRef(null);

  const focus = () => inputRef.current.focus();

  return (
    <div>
      <input ref={inputRef} type="text" placeholder="Введите текст" />
      <button onClick={focus}>Фокус на input</button>
    </div>
  );
}
2. Хранение предыдущего значения
jsx
Копировать код
import { useState, useEffect, useRef } from "react";

function PreviousCounter() {
  const [count, setCount] = useState(0);
  const prevCountRef = useRef(0);

  useEffect(() => {
    prevCountRef.current = count;
  }, [count]);

  return (
    <div>
      <p>Текущее: {count}</p>
      <p>Предыдущее: {prevCountRef.current}</p>
      <button onClick={() => setCount(count + 1)}>+1</button>
    </div>
  );
}
3. Таймер с useRef
jsx
Копировать код
import { useState, useEffect, useRef } from "react";

function Timer() {
  const [seconds, setSeconds] = useState(0);
  const intervalRef = useRef(null);

  useEffect(() => {
    intervalRef.current = setInterval(() => setSeconds(s => s + 1), 1000);
    return () => clearInterval(intervalRef.current);
  }, []);

  return <h1>{seconds} секунд</h1>;
}
🔹 Практическое задание
Создать input и кнопку, при нажатии кнопки устанавливать фокус на input.

Сделать компонент PreviousCounter, который показывает текущее и предыдущее значение счётчика.

Создать таймер с использованием useRef для хранения идентификатора setInterval.

🔹 Советы и лучшие практики
Используйте useRef для сохранения данных между рендерами, не влияя на UI.

Для доступа к DOM всегда используйте ref вместо document.querySelector.

Не используйте useRef как замену state для значений, которые должны вызывать перерендер.

🔹 Дополнительно
useRef часто применяется вместе с useEffect для управления DOM и подписками.

Можно использовать для хранения предыдущих пропсов, таймеров, внешних библиотек.

После освоения useRef переходите к useContext и useReducer для управления глобальным состоянием.

yaml
Копировать код
