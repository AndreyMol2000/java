04.2-Динамические-маршруты.md
# Динамические маршруты в React Router

## 🔹 Кратко
Динамический маршрут — это маршрут, который содержит **параметры**.  
Позволяет показывать разные страницы или данные на основе URL.

```jsx
<Route path="/user/:id" element={<User />} />


:id — параметр, который можно получить через useParams.

🔹 Подробно
1. Определение динамических маршрутов

Используется двоеточие : перед именем параметра.

Можно иметь несколько параметров:

<Route path="/user/:userId/post/:postId" element={<Post />} />


В компоненте параметры доступны через хук useParams.

2. Пример использования
import { BrowserRouter, Routes, Route, useParams, Link } from "react-router-dom";

function User() {
  const { id } = useParams();
  return <h1>Пользователь {id}</h1>;
}

function App() {
  return (
    <BrowserRouter>
      <nav>
        <Link to="/user/1">Пользователь 1</Link> | 
        <Link to="/user/2">Пользователь 2</Link>
      </nav>
      <Routes>
        <Route path="/user/:id" element={<User />} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;


При переходе на /user/1 компонент покажет «Пользователь 1», на /user/2 — «Пользователь 2».

3. Использование нескольких параметров
function Post() {
  const { userId, postId } = useParams();
  return <h1>Пользователь {userId}, Пост {postId}</h1>;
}

<Routes>
  <Route path="/user/:userId/post/:postId" element={<Post />} />
</Routes>

🔹 Практическое задание

Создать страницу пользователей с динамическим маршрутом /user/:id.

Создать несколько ссылок на разных пользователей через <Link>.

Добавить страницу постов с маршрутом /user/:userId/post/:postId.

Получить параметры через useParams и отобразить их на странице.

🔹 Советы и лучшие практики

Используйте параметры только для динамических данных.

Для статических страниц используйте обычные маршруты.

Можно использовать optional параметры с ?: /user/:id?.

После освоения динамических маршрутов переходите к навигации, Redirect и защищённым маршрутам.

🔹 Дополнительно

React Router предоставляет хуки:

useParams — получить параметры.

useNavigate — программная навигация.

useLocation — информация о текущем URL.

Динамические маршруты часто комбинируются с API для подгрузки данных.