# 👻 Анонимные классы и функции в Java

## 📝 Краткая форма

- **Анонимный класс** — это **класс без имени**, создаваемый **прямо в момент создания объекта**.  
- Используется для **переопределения методов или добавления функционала на месте**, без создания отдельного класса.  
- Часто применяется с **абстрактными классами, интерфейсами и наследниками**.  

---

## 📚 Подробная теория

### 1) Пример создания анонимного класса
```java
Car flycar = new Car(100.5f, 2500, "Red", new byte[]{0, 0, 0}) {
    @Override
    public void move(float speed) {
        super.move(speed); // вызов родительского метода

        // добавление кастомного поведения
        this.engine.isReady(true);
        System.out.println("Автомобиль готов к полету!");
    }
};
Здесь создается объект flycar подкласса Car, но класс не имеет имени.

Метод move переопределяется только для этого объекта.

Можно вызывать super для доступа к методам родителя.

2) Зачем нужны анонимные классы
Чтобы не создавать отдельный файл класса для одноразового использования.

Для кастомизации поведения объекта на месте.

Часто применяются с GUI, потоками, обработчиками событий.

3) Использование с объектами
java
Копировать код
public class Main {
    public static void main(String[] args) {

        Car bmw = new Car(100.5f, 2500, "Red", new byte[]{0, 0, 0});
        bmw.engine.setValue(false, 2000);
        bmw.engine.info();

        // Анонимный класс для кастомного поведения
        Car flycar = new Car(100.5f, 2500, "Red", new byte[]{0, 0, 0}) {
            @Override
            public void move(float speed) {
                super.move(speed);
                this.engine.isReady(true);
                System.out.println("Автомобиль готов к полету!");
            }
        };

        flycar.move(300); // вызов переопределенного метода
    }
}
Анонимный класс переопределяет метод move только для flycar.

bmw.move(...) будет использовать стандартный метод класса Car.

4) Подводные моменты
Анонимный класс не может иметь конструктор (но может использовать конструктор родителя через super).

Можно использовать только один раз, присваивая объект переменной.

Полезен для одноразового переопределения методов и локального расширения функционала.

Чаще всего применяется с абстрактными классами и интерфейсами.

💻 Полный пример кода
java
Копировать код
public class Main {
    public static void main(String[] args) {

        // Обычный объект Car
        Car bmw = new Car(100.5f, 2500, "Red", new byte[]{0, 0, 0});
        bmw.engine.setValue(false, 2000);
        bmw.engine.info();

        // Анонимный класс: объект Car с кастомным методом move
        Car flycar = new Car(100.5f, 2500, "Red", new byte[]{0, 0, 0}) {
            @Override
            public void move(float speed) {
                super.move(speed);
                this.engine.isReady(true);
                System.out.println("Автомобиль готов к полету!");
            }
        };

        // Вызов метода анонимного объекта
        flycar.move(300);
    }
}